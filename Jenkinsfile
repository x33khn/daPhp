pipeline {
  agent any
  stages {
    stage('verify version') {
      steps {
        //sh 'php --version'
        println('Hello worlds');
      }
    }
    stage('hello') {
      steps {
        sh 'hello.php'
      }
    }
  }
}
